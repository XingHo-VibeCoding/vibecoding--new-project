# 热浪 TREND WAVE · 前端设计规则

> Day 9 制定。这份文件是「好看」变成「可执行规则」的地方：每次改界面前对照它检查，改完用它验证。
> 手册要求说清三件事：**检查什么、遵守什么、怎么验证**。本文件按这个结构写。

## 一、检查什么（改界面前过一遍）

1. 文字对比度（正文、辅助文字、按钮文字）
2. 键盘焦点与按钮状态（hover / focus / disabled 三态齐不齐）
3. 中文排版（字号、字距、行高）
4. 间距节奏（同一区域的留白是否一个体系）
5. 移动端（<768px 无横向溢出、文字按钮不挤爆）

## 二、遵守什么（硬规则）

### R1 · 对比度
- 小字（<18px）：与背景对比度 **≥ 4.5:1**（WCAG AA）。
- 本项目暗色底 `#0a0e1a` 上的最低可用灰是 `slate-400`（≈7.5:1）；
  `slate-500` ≈ 4.0:1 只能用于 ≥18px 的粗体或装饰性文字；
  **`slate-600` 及更深的灰禁止用于文字**（≈2.5:1）。
- 实测工具：浏览器 DevTools 里肉眼比对 + 开发时按下述 R5 验证。

### R2 · 可交互元素三态
- 所有按钮和链接：`hover` 有反馈（已有）之外，**必须有可见的 `focus-visible` 样式**（键盘用户能看到焦点在哪）——统一用 `focus-visible:ring-2 ring-orange-400/60 focus-visible:outline-none`。
- disabled 按钮必须加 `cursor-not-allowed opacity-60`，不能看起来还能点。

### R3 · 中文排版
- **中文标签不用 `uppercase`（无效）也不用 `tracking-wider`**（汉字间距被拉松，显得稀）。中文小标签用 `text-xs font-medium text-slate-400` 即可；`tracking-*` 只允许用在纯英文文本上。
- 正文行高保持 `leading-relaxed` 以上；12px（`text-xs`）是中文最小字号，再小不可读。

### R4 · 间距节奏
- 全部用 Tailwind 4px 网格（已有）。同一容器内**相邻区块的纵向 padding 必须来自同一节奏**（如 py-5 → pb-4 → pb-6 属于乱拍，应统一为 4/8 的倍数递进）。

### R5 · 移动端
- <768px：无横向滚动条；固定定位的浮层不得超出视口宽度；大标题要降级（`text-2xl sm:text-3xl`）。

## 三、怎么验证（改完后必做）

1. `npm run dev` 打开页面，桌面宽度肉眼看一遍五项检查。
2. 缩窄到手机宽度（<768px）再过一遍 R5。
3. 用 Tab 键在页面上走一圈，确认每个可交互元素都有可见焦点框（R2）。
4. 截图留证：修前 / 修后各一张，同一位置对比（Day 9 起的习惯）。

## 附：为什么是这五条

Day 8 补齐了功能状态（加载/成功/空/错误），但「像不像正经产品」取决于视觉状态——
用户不会说「对比度不够」，只会说「看着不舒服、有点假」。这五条就是把「不舒服」翻译成能逐条修的规则。
