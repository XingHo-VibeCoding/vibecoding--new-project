# PRD：热浪 TREND WAVE · 多平台热搜聚合站

> 依据：`research.md`（Day 3 需求研究）+ 用户 Day 5 共创决策。
> 性质：产品总纲，**不含任何技术实现细节**（技术选型见 Day 5 `TECH_DESIGN.md`）。
> 版本：v0.2（Day 5 共创修订；v0.1 为 Day 4 初版，已被本版覆盖）。

---

## 一、产品概述

**一句话**：一个多平台热搜聚合站「**热浪 TREND WAVE**」，用户在同一个页面查看 3 个平台的热搜，每平台 TOP 50，并按自己的偏好选平台、筛标签、收藏感兴趣的内容，登录后选择会跨设备记住。

**解决什么问题**：想看各平台热搜，得开 3 个 App 或网页来回切换；现有聚合站（如今日热榜）虽然内容全，但只能被动滚动浏览，**缺少筛选和个性化的能力**（Day 3 研究的关键洞察）。

**差异化定位**：不拼「聚合数量」（tophub 有 30+ 平台），拼「看得主动」——用户自己选平台、自己筛标签、自己收藏，登录后选择跨设备保留，把「刷热搜」变成「挑热搜」。

**品牌**：热浪 TREND WAVE（中英双语，中文为主）。视觉基调参考草稿 https://silver-pothos-b953be.netlify.app/ —— 雷达站式叙事 + 数据仪表盘感 + 实时感。

---

## 二、目标用户

| 用户 | 场景 | 核心诉求 |
|---|---|---|
| 新媒体/运营从业者 | 早上用 10 分钟快速掌握多平台热点 | 多平台一眼看完，不漏热点 |
| 关注特定话题的普通用户 | 想知道某事件（如某赛事、某产品）在各平台的讨论 | 按关键词 + 标签过滤，直达相关条目 |
| 学生/研究者 | 追踪舆情、写报告找素材 | 收藏有用条目，登录后跨设备保留 |

**共同点**：都不想装 3 个 App 来回切，都不想在一屏 50 条里肉眼找东西。

**明确不是目标用户**：需要监控级实时数据（分钟级告警）的专业机构、想要 App 端体验的移动端重度用户。

---

## 三、MVP 功能（本期必做）

> 每个功能都按「用户动作 → 可观察结果」写。技术实现方式一律不在这里出现。

### F1｜首页 3 列布局（每平台一列）

- **用户动作**：打开网站首页，不做任何操作。
- **可观察结果**：看到 **3 列并排**，**每个选中平台各占一列**，每列顶部带平台名（微博/知乎/抖音）+ 当前排名区间（如「1–50」）；每列**默认展示前 10 条**，每条含排名、标题、热度值、分类标签（娱乐/社会/科技/财经/体育/游戏中的一种）；每列底部有「**展开剩余 40 条**」按钮，点击后该列展开全部 50 条；每条有「X 分钟前更新」的时间标注。
- **3 列上限**：用户最多同时展示 3 个平台（6 选 3）；当只选 1 个或 2 个平台时，对应列数自动减少。
- **不做**：自动轮播、推送、通知。

### F2｜平台个性化选择（6 选 3，持久化）

- **用户动作**：在首页「选择展示的平台」区，勾选 1–3 个平台（共 6 个可选：微博 / 知乎 / 抖音 / B站 / 百度 / 小红书）。
- **可观察结果**：勾选状态立即同步——多余 3 个时最旧的自动取消；选完后 F1 的列布局立即按新选择重排；右上角显示「已选 3 / 共 6」计数；选中卡片用品牌色高亮。
- **默认**：未登录用户首次访问默认 **微博 / 知乎 / 抖音**；登录用户从数据库读取上次选择。
- **持久化**：
  - 未登录 → 浏览器 `localStorage`
  - 登录后 → 同步到 PostgreSQL，下次进入任何设备都按个人偏好取
- **不做**：拖拽排序、平台自定义（添加第 7 个）、平台搜索。

### F3｜标签筛选（6 类，持久化）

- **用户动作**：点击 F1 上方的分类标签（全部 / 娱乐 / 社会 / 科技 / 财经 / 体育 / 游戏）。
- **可观察结果**：3 列热搜列表同时按所选标签过滤——选「全部」不过滤，选「科技」则只显示带「科技」标签的条目；过滤后若某列无结果，该列显示「该分类下暂无热搜」+ 切回「全部」入口；多选支持（点多个标签按「任一匹配」）。
- **标签体系**：6 类按热度从大到小排：**娱乐 / 社会 / 科技 / 财经 / 体育 / 游戏**（娱乐/社会/科技为基础三大类，财经/体育/游戏为补充三大类）。
- **持久化**：同 F2（localStorage → 登录后同步 DB）。
- **不做**：自定义标签、标签搜索、标签热度趋势。

### F4｜详情查看

- **用户动作**：点击列表中的任意一条热搜。
- **可观察结果**：进入该条目的详情页，显示完整标题、所属平台、当前排名、热度值、状态标签（如「沸」「新」「爆」）、分类标签、原文链接；点击原文链接可跳转到来源平台。
- **不做**：站内评论、内容摘要、相关推荐。

### F5｜收藏（未登录 localStorage，登录后跨设备同步）

- **用户动作**：在列表或详情页点击条目旁的 ☆ 图标；再次点击取消。
- **可观察结果**：☆ 变为实心 ★（已收藏）；进入「我的收藏」视图能看到所有已收藏条目，按收藏时间倒序；取消后从收藏列表消失。
- **持久化**：
  - 未登录 → `localStorage`，刷新或重开浏览器仍在
  - 登录后 → 同步到 PostgreSQL，跨设备可见
- **不做**：收藏夹分组、收藏备注、导出。

### F6｜用户登录 + 7 天免密

- **用户动作**：
  - 首次登录：点击右上角「登录」→ 输入账号密码 → 进入个人状态
  - 7 天内再次打开：自动恢复登录态，**无需输入密码**
  - 7 天后：提示「请重新登录」，再次输入密码
- **可观察结果**：
  - 登录后右上角显示用户名 + 「退出」按钮
  - 登录后所有个性化选择（平台/标签/收藏）从数据库读取并跨设备保留
  - 退出后回到本地 localStorage 模式
- **安全要求**：
  - 密码**不**明文存储（哈希）
  - 「免密」靠服务器签发的临时 token，过期 7 天，token 存在浏览器 cookie 或 localStorage
  - 退出时清空本地 token
- **不做**：注册（手动后台写账号；MVP 阶段用户不多）；找回密码；第三方登录（GitHub/微信等）；多设备会话管理（同一账号后登录挤掉先登录）。

### F7｜四种状态（稳定性底线）

每个数据加载场景（首页、切换平台、详情页、登录、个人偏好同步）都必须能表现：

| 状态 | 用户看到什么 |
|---|---|
| 加载中 | 骨架屏或「加载中…」，不是白屏 |
| 加载成功 | 正常列表 |
| 列表为空 | 「该平台/分类暂无热搜」+ 引导文案，不是空白 |
| 出错 | 「加载失败，点击重试」按钮，不是报错代码 |

---

## 四、后续功能（明确不在本期）

| 功能 | 为什么现在不做 | 什么时候做 |
|---|---|---|
| 注册 / 找回密码 / 第三方登录 | MVP 用户数小，手动后台写账号够 | v1.1 |
| 多设备会话管理（挤下线） | 单设备登录已够 MVP | v1.1 |
| 历史趋势（24h/7d 曲线） | 需要持续存历史数据，工作量大 | v1.1 |
| 跨平台同事件排名对比 | 数据对齐复杂，超出 MVP | v1.1 |
| 推送/邮件订阅 | 需要服务端定时任务 | v1.1 |
| 移动端 App | PWA 都不做 | v2.0 |

**永久不做**：商业化（广告/联盟链接/热搜买卖）、AI 推荐算法、模糊匹配/拼音搜索、收藏备注。

---

## 五、页面与交互要求（4 个视图）

### V1｜首页（3 列布局）

- 布局：顶部品牌 + 大标题钩子 + 3 个数据指标（实时条数 / 平台数 / 刷新频率）+ 「选择展示的平台」区（6 选 3）+ 搜索框（保留）+ 6 标签筛选 + 3 列热搜
- 交互：平台勾选立即重排；标签筛选 3 列同时过滤；条目点击进详情；☆ 收藏
- 桌面浏览器优先（3 列天然适合桌面）；移动端浏览器单列堆叠（不专门优化，能看能用）

### V2｜详情页

- 内容：单条热搜完整信息 + 原文链接跳转按钮 + 收藏按钮
- 入口：仅从首页列表点击进入；有「返回列表」入口

### V3｜我的收藏（视图，不单独路由）

- 内容：已收藏条目列表，按收藏时间倒序
- 交互：可取消收藏；点击条目仍可进详情
- 空状态：「还没有收藏，去首页看看」引导

### V4｜登录态/退出入口

- 内容：右上角头像/用户名（登录后）/ 「登录」入口（未登录）
- 交互：登录弹窗（账号密码）/ 退出确认

---

## 六、数据字段

### 6.1 热搜条目 HotItem

| 字段 | 类型 | 说明 | 示例 |
|---|---|---|---|
| id | string | 唯一标识 | `weibo_20260920_0001` |
| platform | enum | 所属平台 | `weibo` / `zhihu` / `douyin` / `bilibili` / `baidu` / `xiaohongshu` |
| rank | int | 当前排名 | `1` |
| title | string | 热搜标题 | `某台风登陆` |
| hotValue | number | 热度值 | `48720000` |
| tag | string? | 状态标签（可空） | `沸` / `新` / `爆` |
| **category** | **enum** | **分类标签** | **`ent` / `soc` / `tech` / `fin` / `sport` / `game`** |
| url | string | 原文链接 | `https://s.weibo.com/...` |
| updatedAt | datetime | 更新时间 | `2026-09-20 10:00` |

> 数据库从 Day 19 起开始持久化历史；MVP 阶段榜单在云函数内存里跑。

### 6.2 收藏记录 Favorite

| 字段 | 说明 |
|---|---|
| userKey | 未登录用户的浏览器本地 ID；登录用户为 userId |
| itemId | 对应的 HotItem.id |
| savedAt | 收藏时间 |

### 6.3 用户 User（登录后启用）

| 字段 | 说明 |
|---|---|
| id | 用户 ID |
| username | 用户名（唯一） |
| passwordHash | 密码哈希（**不存明文**） |
| createdAt | 注册时间 |
| tokenExpiresAt | 当前免密 token 过期时间（7 天有效） |

### 6.4 用户偏好 Preference（登录后启用）

| 字段 | 说明 |
|---|---|
| userId | 关联 User.id |
| selectedPlatforms | 选中的平台数组（最多 3 个） |
| selectedCategories | 选中的标签数组（可空 = 全部） |
| updatedAt | 上次修改时间 |

---

## 七、异常状态

| 场景 | 用户看到 | 用户能做什么 |
|---|---|---|
| 数据加载失败 | 「加载失败」+ 重试按钮 | 点重试 |
| 某平台无数据 | 「该平台暂无热搜」+ 引导 | 切其他平台 |
| 筛选无结果 | 「该分类下暂无热搜」+ 切回「全部」 | 一键切回 |
| 登录失败 | 「账号或密码错误」 | 重输或退出登录 |
| Token 过期 | 「请重新登录」 | 重新输入密码 |
| 个人偏好同步失败 | 降级使用 localStorage | 提示「离线模式」 |

**底线**：任何情况下不允许出现白屏、裸报错代码（如 JSON 堆栈）。

---

## 八、验收标准（逐条可勾选）

### 功能验收

**F1 / 3 列布局**
- [ ] 打开首页默认 3 列（微博/知乎/抖音），每列显示前 10 条
- [ ] 每列底部「展开剩余 40 条」按钮工作正常
- [ ] 只勾选 1 个或 2 个平台时，列数相应减少

**F2 / 平台选择**
- [ ] 6 个平台可勾选，勾选超过 3 个时最旧的自动取消
- [ ] 勾选后 F1 立即重排
- [ ] 默认（未登录首次访问）是微博/知乎/抖音
- [ ] localStorage 持久化生效，刷新页面后选择仍在

**F3 / 标签筛选**
- [ ] 6 个标签（全部/娱乐/社会/科技/财经/体育/游戏）点击立即过滤
- [ ] 多选支持（任一匹配）
- [ ] 过滤后某列为空时显示引导文案

**F4 / 详情**
- [ ] 点击任意条目进入详情，能看到完整字段和原文链接

**F5 / 收藏**
- [ ] 收藏成功，「我的收藏」能看到
- [ ] 刷新后收藏仍在
- [ ] 取消收藏后从收藏列表消失

**F6 / 登录 + 7 天免密**
- [ ] 登录成功后右上角显示用户名
- [ ] 7 天内重新打开，**无需输密码**直接进入登录态
- [ ] 7 天后提示重新登录
- [ ] 退出后回到本地 localStorage 模式
- [ ] 密码不以明文形式出现在任何代码、提交、截图

### 状态验收

- [ ] 首页有加载中状态（非白屏等待）
- [ ] 断网/接口失败时显示「加载失败 + 重试」
- [ ] 空列表场景显示引导文案，非空白

### 边界验收

- [ ] 未实现任何「后续功能」清单里的功能
- [ ] 页面无广告、无联盟链接
- [ ] 密码哈希不存储明文

### 完工验收（手册口径）

- [ ] `PRD.md` 已提交入库
- [ ] 含本验收标准章节
- [ ] AI 自检未发现「必须修改」级问题

---

## 附：AI 自检记录

| 检查项 | v0.1（Day 4） | v0.2（Day 5 共创后） |
|---|---|---|
| 8 个必备板块齐全 | ✅ | ✅ |
| 每个功能有「用户动作+可观察结果」 | ✅ | ✅（F1–F7 全部双栏） |
| MVP/后续有分界 | ✅ | ✅（登录已从后续上移到 F6） |
| 至少 3 个页面/视图 | ✅（3 个） | ✅（4 个：首页+详情+收藏+登录态） |
| 无技术实现细节泄漏 | ✅ | ✅（仍不含技术栈，仅数据字段形态） |
| 反映 Day 5 共创决策 | — | ✅（6 选 3 / 6 标签 / 3 列布局 / 登录 7 天免密 / 热浪品牌） |